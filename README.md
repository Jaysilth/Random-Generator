# 🎲 Random Generator (Java)

A simple Java console program that can:

1. Generate random numbers
2. Simulate a coin flip (heads or tails)

---

## 💡 What I Learned
- How to use the **`Random` class** in Java
- How to generate **random integers** with `nextInt()`
- How to generate **random boolean values** with `nextBoolean()`
- How to use **if-else statements** to handle different outputs

---

## ▶ How to Use
1. Run the program (`randomGenerator.java`) in IntelliJ or terminal
2. Uncomment the first part to generate **3 random numbers**
3. The second part simulates a **coin flip** and prints the result

---

## 📂 Project Structure
